apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: devicehub
  namespace: argocd
spec:
  description: DeviceHub mobile lab workloads
  sourceRepos:
    - '*'
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
